<%= form_for(@assistance) do |f| %>
  <% if @assistance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assistance.errors.count, "error") %> prohibited this assistance from being saved:</h2>

      <ul>
      <% @assistance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.collection_select :user_id, User.all, :id, :nombre, {class: "form-control input-sm"} %>
  </div>
  <div class="field">
    <%= f.label :salida %><br>
    <%# f.datetime_select :salida %>
  </div>
  <div>

  </div><%= f.hidden_field :entrada, :value => Date.today %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
