<div class="row clearfix">
	<div class="col-md-5 column">

	</div>
	<div class="col-md-4 column">
		<h2>Iniciar sesion</h2>	
	</div>
</div>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
<div class="container">
	<div class="row clearfix">
		<div class="col-md-4 column">
		</div>
		<div class="col-md-4 column">
			<div class="well">
			<form role="form">
				<div class="form-group">
					 <label for="exampleInputEmail1">Direccion de correo electrónico</label><%= f.email_field :email, :autofocus => true, :class => "form-control", :placeholder => "Correo electronico" %>
				</div>
				<div class="form-group">
					 <label for="exampleInputPassword1">Contraseña</label><%= f.password_field :password, :class => "form-control" %>
				</div>
					 <% if devise_mapping.rememberable? -%>
    					<div><%= f.check_box :remember_me %> <%= f.label :remember_me, "Mantener la sesión iniciada" %></div>
  					<% end -%>


				<button type="submit" class="btn btn-primary">Iniciar sesión</button>
			</form>
		</div>
		</div>
	</div>
</div>
<% end %>

<%= render "devise/shared/links" %>